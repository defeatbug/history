# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主要用户：历史学习者（学生）**
在碎片时间用手机学习历史。使用场景是「打开就刷几道题」而不是「坐下来读一章」，
因此对单次会话的时长、加载速度、题目切换的流畅度敏感。

**次要用户：内容管理员（单一角色）**
本项目的管理员只有一人（毕设作者）。职责是录入与维护历史课程、题目、事件、博物馆
与文物，并控制内容的展示顺序。使用场景以桌面端批量录入为主，但**也需要在手机上
完成轻量维护**（改错别字、换封面图、调整排序）。

## Product Purpose

HistoriaQuest 是一个游戏化的历史学习平台。它把历史知识拆成课程与题目，
用答题、勋章、错题本、断点续学把「学习历史」变成可持续的日常行为。

成功的标志：学生能持续回来学习，且答错过的题在遗忘前被重新复习到。

## Positioning

与「历史知识科普类产品」的区别在于**它不假设用户会主动回来**：

- 用 **SM-2 间隔重复算法**在遗忘临界点主动安排复习，而不是等用户想起来
- 用**错题本 + 掌握度进度**把「我学过」变成可量化的过程
- 用**断点续学**保证任何一次中断都能无缝接上

同类产品可以照抄课程内容与题型，但抄不走「基于个体作答历史的复习调度」。

## Operating Context

**学生侧**
- 主要设备：手机
- 典型路径：首页 → 继续学习（断点）→ 答题 → 完成后看正确率
- 复习路径：错题本 → 查看待复习数量 → 进入复习 → 答题后看到「下次复习时间」
- 辅助：AI 历史助手（需登录）、博物馆与文物浏览、历史事件检索、时间轴

**管理侧**
- 设备：桌面为主，手机为辅
- 典型路径：进入后台 → 选内容类型 → 搜索/筛选定位 → 编辑表单 → 保存
- 关键动作：调整内容展示顺序（拖拽）、上传图片、删除内容前确认影响面
- 管理员是内容质量的唯一责任人，**没有审核流与草稿态**

## Capabilities and Constraints

**已实现**
- 内容：14 门课程 / 102 道题 / 15 个历史事件 / 8 座博物馆 / 13 件文物 / 10 个勋章
- 认证：邮箱注册（含邮箱确认）、登录、游客模式
- 学习：答题、正确率、学习时长、断点续学、进度云端同步
- 复习：错题本、SM-2 间隔重复调度、掌握度进度
- AI：通过 Edge Function 代理的服务端流式对话（密钥不出服务端）
- 数据：PostgreSQL + 行级安全策略（RLS）、内容变更即时生效

**规划中（本期后台）**
- 内容管理后台：课程与题目的完整增删改查（第一批），
  历史事件 / 博物馆 / 文物（第二批），勋章仅可改文案
- 学习进度统计：管理员可查看全量学生的学习概览（完成课程数、学习时长、
  正确率、最后活跃时间），并配 1~2 个趋势图表。**只读，不可修改学生数据**
- 图片上传：单一 public bucket，前端压缩后上传
- 排序：拖拽调整展示顺序

**明确不做（已评估后否决）**
- 多管理员与用户管理界面（改角色、删用户）—— 单管理员场景下无实际需求，
  且会引入提权风险。注意与「学习进度统计」的区别：**统计是只读的，
  不提供任何修改学生数据或权限的入口**
- 草稿 / 发布状态机 —— 单管理员场景无审核需求，且会让所有内容查询都要加过滤条件
- 软删除 —— 理由同草稿态
- 内容分页 —— 最大数据集仅 102 行，分页收益为零
- 批量操作 —— 误操作风险高于收益
- 多题型（填空 / 情境 / 连线） —— **前端尚不具备渲染能力**，
  后台若开放配置会产生「配了但显示不出来」的陷阱
- 勋章解锁规则的图形化配置 —— 规则维护在数据库函数内，界面开放会造成误导

**技术约束**
- 内容表以业务可读文本作主键（如 `lesson-1`），用户行为表用 UUID
- 题目的选项与答案以 `jsonb` 存储，答案当前为单个数字索引（单选）
- 删除内容会通过外键级联影响学生的答题与复习记录，必须前置提示
- `completed_lessons` 是无外键的文本数组，删除课程后需清理悬空 id

## Brand Commitments

- **名称**：HistoriaQuest，中文语境下可称「历史探索」
- **语言**：界面文案为简体中文，语气友好但不幼稚
- **图标语言**：课程与博物馆封面使用 emoji 作为视觉标识，是本项目已成立的约定
- 现有学习端的视觉系统（暖色渐变、大圆角卡片）是既定事实；
  管理端是否沿用由界面设计另行决定，本文件不作规定

## Evidence on Hand

- `src/data/mockData.ts`、`src/data/chinaHistory.ts`、`src/data/museumData.ts` 内含真实
  内容数据，可作为后台表单的样例与校验依据
- `public/images/` 内含 10 张本地图片，其中 3~4 张被文物数据引用，
  但路径写作 `../../public/images/xxx.jpg`，在浏览器中会 404
- 文物数据的 `imageUrl` 混用本地路径与外部链接，风格不统一
- **不存在**：用户评价、使用数据统计、性能基准、媒体报道。
  任何面向用户的界面都不得编造此类内容

## Product Principles

1. **不假设用户会主动回来。** 任何「让学生持续学习」的机制都应当由系统主动触发
   （复习提醒、断点续学），而不是依赖用户的自觉。
2. **后台不做前端支撑不了的能力。** 管理端开放给管理员的每个配置项，
   都必须能被学生端正确渲染。宁可先不开放，也不制造「配了没用」的陷阱。
3. **删除是不可逆的，所以必须被认真对待。** 任何会波及学生数据的操作，
   都要在提交前告知影响范围，而不是事后让管理员发现。
4. **内容即数据，顺序即信息。** 管理员必须能控制内容的展示顺序，
   否则新增内容会随机打乱既有的教学编排。
5. **降级要好过空白。** 图片缺失时回退到 emoji，后端不可用时回退到本地数据 ——
   半成品状态不能让用户看到破图或空屏。

## Accessibility & Inclusion

- 学生端以手机为主要设备，**所有界面必须适配窄屏**
- 管理端同样需要手机可用，但允许以卡片式布局降级替代宽表格
- 表单控件需具备可读的标签与错误提示位置，不能仅靠颜色传达状态
- 内容以中文为主，字体大小需保证手机上无需缩放即可阅读

## Privacy

管理端可读取学生的学习数据（进度、答题记录），但遵循**最小必要原则**：

- **不展示邮箱等个人身份信息**，以用户名为唯一标识
- **只读**：管理端不提供任何修改学生进度、答题记录或角色的入口
- 学生端仍然只能读自己的数据；管理员的全量读权限通过独立的 `is_admin()`
  策略授予，而不是放宽原有策略
